import { LogMessage, LogTable } from './interfaces';
export declare function Log(...messages: (string | LogMessage)[]): void;
export interface LogTableOptions {
    padding?: number;
    spacing?: number;
}
/**Logs a table in node. */
export declare function LogTable(table: LogTable, options?: LogTableOptions): void;
